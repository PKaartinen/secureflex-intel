"""
SecureFlex Intel — AI-Powered Lead Intelligence Pipeline

Discovers, enriches, scores, and monitors potential security contract
leads from UK public data sources. Designed to run standalone or be
integrated into a web server / dashboard.

Modules:
    sources     — Data source connectors (Contracts Finder, Companies House, etc.)
    pipeline    — Lead processing (enrichment, scoring, classification)
    output      — Report generation and data export
    config      — Configuration and environment management
    models      — Shared data models
"""

__version__ = "0.1.0"
